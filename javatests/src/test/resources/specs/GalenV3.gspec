@objects
  container     css      div

= container =
  container:
    centered horizontally inside viewport 

  @on mobile, tablet
    container:
      inside viewport 0px top left right
  
  @on desktop
    container:
      inside viewport 80px top 
